import type { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Support with documents',
};


export default function SupportPage(){
    return (
        <div>
            <h1 className="text-xl font-bold text-center">Support with documents</h1>
        </div>
    )
}